2026-09-08T17:40:11Z

# Emerald — Plan of Plans

Tracks which `id-workflow` plans, taken together, implement
[`2026-09-08T173600Z-inception.md`](./2026-09-08T173600Z-inception.md).

This document is an index only. It does not author plan content — each row
becomes its own PLAN-mode deliverable (a Maestro `pln-*` artifact) authored
separately when its turn comes. Update **Status** here as each plan is
authored, executed, and shipped; do not let plan detail leak back into this
file.

## Status legend

| Status | Meaning |
|---|---|
| `not-started` | Row exists, no plan authored yet |
| `planned` | Plan authored (Maestro `pln-*`), not yet approved/executing |
| `in-progress` | Approved and in EXECUTE |
| `done` | Shipped; REVIEW passed |

## Plans

| # | Plan | Scope | Inception §§ | Depends on | Status |
|---|---|---|---|---|---|
| 01 | `spec-foundations` | Author `GRAMMAR.md`, `SEMANTICS.md`, `TYPE_SYSTEM.md`; resolve the open semantic questions (variables, nil, methods, classes, blocks, arrays, exceptions, numerics, strings, modules) | §4, §19, §25.A–C | — | done |
| 02 | `toolchain-prototype` | Prototype and select lexer/parser/codegen crates: logos, LALRPOP vs chumsky, Cranelift vs Inkwell; decide before committing | §14, §23 | 01 | done |
| 03 | `workspace-bootstrap` | Cargo workspace scaffold (crates, grammar/, spec/, examples/, tests/, benchmarks/) | §16 | 01 | done |
| 04 | `milestone1-front-end` | Lexer + parser + AST/syntax tree for the `add(a, b)` vertical slice | §17, §25.D | 02, 03 | done |
| 05 | `milestone1-typecheck` | Name resolution + type checker; reject `add(a: Int64, b: String)` with a useful diagnostic | §17, §25.E | 04 | done |
| 06 | `milestone1-codegen` | Native codegen, link, run; `add(20, 22)` prints `42` | §17, §25.F | 05 | done |
| 07 | `control-flow` | Locals, `if`, comparisons, loops, `case`, `return`/`break`/`next` | §5, §17 (2nd slice) | 06 | done |
| 08 | `object-model` | Classes, declared fields, inheritance, static method dispatch — `Point` example | §8, §17 (3rd slice) | 07 | done |
| 09 | `collections` | `Array[T]` / `Hash[K, V]`, unboxed primitive array representation | §10 | 06 | done |
| 10 | `blocks-closures` | Blocks/lambdas/procs, closure scoping and escape rules | §19 (Blocks) | 07 | done |
| 11 | `exceptions` | Exception model, native-unwinding implementation | §19 (Exceptions) | 06 | done |
| 12 | `modules-mixins` | Module `include`/`extend` semantics, multiple-inheritance rule | §19 (Classes/Modules) | 08 | done |
| 13 | `diagnostics` | `miette`-based error reporting to the quality bar shown in §14.4 | §14.4 | 02 | done |
| 14 | `id-effect-integration` | Adopt `id_effect` for pipeline orchestration/context wiring where it clarifies the architecture, not everywhere | §15 | 03 | done |
| 15 | `benchmarking` | Benchmark suite (sum, sum-of-squares, fib, matmul, traversal, string, allocation, dispatch, recursion) vs Rust/C/C++/Ruby; report runtime, binary size, compile time, memory | §11, §21 | 07, 09 | done |

## Completion (v1)

Emerald v1, as envisioned in the inception document, is complete when every
row above is `done`. As of this writing, all 15 v1 rows are `done`.

## Post-v1 plans

Work opened after v1 shipped: performance follow-through on the inception
document's own revisit trigger, then a self-extending backlog where each
plan's own Decision log or Out-of-scope section disclosed the next plan.
None of these map to an inception `§§` — column entries below say `post-v1`
instead. Status is inferred from `git log` (a plan with a shipping commit
is `done`) and from working-tree diffs (uncommitted code against a plan's
own AST/grammar shape is `in-progress`); update rows here as each plan
actually ships instead of re-deriving status from the tree.

| # | Plan | Scope | Inception §§ | Depends on | Status |
|---|---|---|---|---|---|
| 16 | `codegen-backend-bakeoff` | Cranelift vs LLVM bake-off, triggered by plan 15's revisit clause (Cranelift measured 5-18x slower than LLVM/hand-written code on `sum`/`array_traversal`) | post-v1 | 15 | done |
| — | `consolidate-llvm-backend` | Remove Cranelift entirely; bring LLVM up to full parity (functions, classes, lambdas/closures, exceptions, modules, arrays, control flow) | post-v1 | 16 | done |
| 17 | `ide-integration` | `emerald-driver` library extraction, TextMate grammar, LSP server, MCP server — syntax coloring and live diagnostics/tool access in an editor | post-v1 | 06 | done — stale label, corrected 2026-09-21 by direct source audit: `emerald-driver`, `emerald-lsp`, and `emerald-mcp` crates are all real, present, and exercised throughout this session's own work (e.g. `compile_and_run_actually_compiles_links_and_runs_hello_em`) |
| 18 | `arithmetic-and-logical-operators` | Binary `- * / %`, unary `- !`, short-circuit `&& \|\|` — operators plan 11 and plan 15 both had to route around | post-v1 | 11, 15 | in-progress (AST + grammar landed, uncommitted; sema/codegen not yet touched) |
| 19 | `string-literals` | Real `"..."` string literal syntax end to end (lexer, grammar, AST, sema, codegen) | post-v1 | 15 | done — stale label, corrected 2026-09-21: real `"..."` syntax (`StringLitTok`) end to end, used in essentially every example this session touched |
| 20 | `comments-and-case-when` | `#` line comments and a `case`/`when` value-match statement — found while authoring plan 17 | post-v1 | 17 | done, under different final syntax — stale label, corrected 2026-09-21: `#` comments shipped exactly as scoped (grammar's comment-skip rule cites this plan by name); the value-match STATEMENT capability shipped too, but plan 71's do-end unification later renamed it from `case`/`when` to `match`/`do` — this row's own named syntax no longer exists even though its described capability does |
| 21 | `lsp-symbols-and-navigation` | Real symbol table in `emerald-sema`; go-to-definition, completion, semantic tokens in `emerald-lsp` | post-v1 | 17 | done — stale label, corrected 2026-09-21: `goto_definition`/`completion_items`/`semantic_tokens_legend` all real in `emerald-lsp` |
| 22 | `sema-diagnostic-spans` | Real byte-offset spans through `Expr`/`Stmt` so sema diagnostics point at the exact sub-expression | post-v1 | 13, 17 | planned |
| 23 | `multi-file-compilation` | `require <path>` for multi-file programs — path resolution, cycle detection, AST-level merge | post-v1 | 06 | done |
| 24 | `tree-sitter-grammar` | Real tree-sitter grammar (`grammar.js`, generated parser, highlight queries, corpus tests) — native highlighting in Zed/Neovim | post-v1 | 17 | done — stale label, corrected 2026-09-21: real `tree-sitter-emerald/grammar.js`, generated parser, highlight queries, and corpus tests all present, plus a compiled `emerald.so` |
| 25 | `stdlib-expansion` | `true`/`false` literals, narrowly-scoped `nil`, `Hash[K, V]` literals/get/set, `Array.new(size)` | post-v1 | 07, 08, 09 | done — stale label, corrected 2026-09-21: `true`/`false`, `Hash[K,V]` literals, and `Array.new(size)` all real and pervasively used (`nil` was later removed outright by plan 73's `Option[T]`, a further supersession beyond this row's own original narrower scope, not a contradiction of it) |
| 26 | `parser-error-recovery` | Multi-error parse recovery at top-level `Item` boundaries, so one pass reports every syntax error in a file | post-v1 | 21 | done — stale label, corrected 2026-09-21: the grammar's own `errors: &mut Vec<...>` parameter threads real multi-error recovery through parsing — a single parse genuinely can and does return more than one `ParseError` (observed directly this session) |
| 27 | `packaging-and-release` | `cargo-dist` GitHub Releases for `emerald-cli`/`emerald-lsp`/`emerald-mcp`, runtime path-lookup fix, reviewed npm/vsce CI job | post-v1 | 17, 21 | planned |
| 28 | `bitwise-operators` | `& \| ^ ~ << >>` on `Int64` — left aspirational by plan 18's Out-of-scope section | post-v1 | 18 | done |
| 29 | `control-flow-completeness` | `elsif` chains and `unless`/`until`; modifier/postfix forms investigated and declined | post-v1 | 07 | done |
| 30 | `for-in-iteration` | `for <var> in [literal array] ... end`, desugared to existing index-based `while` machinery | post-v1 | 07, 09 | done |
| 31 | `compound-and-multiple-assignment` | `+= -= *= /= %=` and fixed-arity multiple assignment (`a, b = b, a`), plus bare-local reassignment | post-v1 | 07 | done |
| 32 | `class-inheritance` | Single-inheritance `class Dog < Animal ... end` — inherited fields/method resolution, same-signature override, still statically dispatched | post-v1 | 08 | done |
| 33 | `field-access-sugar` | Opt-in `read` field marker synthesizing a zero-arg accessor (`attr_reader`-equivalent) | post-v1 | 08, 11 | done |
| 34 | `blocks-and-yield` | Implicit block arguments (`method(args) { \|x\| ... }`) and `yield`, distinct from plan 10's explicit `Proc` values | post-v1 | 10 | done |
| 35 | `debug-info` | Real DWARF line-table info in the emitted object file, proven with a scripted debugger session | post-v1 | 16 | done — stale label, corrected 2026-09-21: real DWARF line-table info via inkwell's `DebugInfoBuilder` (`compile_to_object_with_debug_info`) |

### Batch: closing the gap to ~45% (plans 36-47)

A prior analysis measured plans 01-35's shipped/target surface at
roughly 10-15% of standard Ruby's language-and-stdlib surface, and set
a strategic ceiling: close every further gap that is compatible with
Emerald's identity (no `method_missing`/`eval`/`send`/reflection, no
mixins/open classes/monkey-patching, no dynamic/virtual dispatch or
vtables, no tracing garbage collector, no runtime reflection), decline
the rest permanently. These twelve independent sibling plans — same
posture as the 28-35 batch — are that closing set, authored together in
one pass. All twelve are `planned` (authored, none executed yet).

One cross-plan inconsistency surfaced during authoring and was
corrected in place rather than left to rot: plan 42 was drafted assuming
plan 37 would deliver a first-class `Range` value; plan 37, once
actually authored, deliberately declined that (`..`/`...` are
grammar-restricted to a `for...in` scrutinee only, no standalone
`Range` value or type). Plan 42 now carries an explicit
"Post-authoring correction" note superseding its own stale `Range`-as-
`Iterable` references; its `Range.each` acceptance criterion is void.

| # | Plan | Scope | Inception §§ | Depends on | Status |
|---|---|---|---|---|---|
| 36 | `string-interpolation-and-heredocs` | `"...#{expr}..."` interpolation (compile-time-parsed, builtin-type stringification only) and `<<~IDENT` squiggly heredocs | post-v1 | 19 | planned |
| 37 | `ranges-and-iteration` | `a..b`/`a...b` over `Int64`, legal only as a `for...in` scrutinee — no first-class `Range` value, no `Range#each`, no slicing | post-v1 | 09, 30, 18 | done — stale label, corrected 2026-09-21: `a..b` over `Int64` as a `for...in` scrutinee (`ForRange` grammar production) exactly as scoped |
| 38 | `full-exception-model` | `ensure` (all exit paths), N ordered typed `rescue` clauses with plan-32 subtype matching, bare `rescue => e`, `retry` | post-v1 | 11, 32 | done — stale label, corrected 2026-09-21: `ensure`/`retry` both real (`Ensure`/`Retry` AST nodes) alongside the rescue/typed-rescue machinery |
| 39 | `function-signature-completeness` | Literal-only default params, compile-time keyword args, homogeneous typed splat, fixed-arity multi-return via a real tuple return type | post-v1 | 06, 31 | done — stale label, corrected 2026-09-21: default params, keyword args, and splat params all real (`splat_param`/`defaults`/`CallKw`) |
| 40 | `operator-overloading` | User classes define `+ - * / == <=> [] []=`; routed through existing static receiver-declared-type method lookup, no new dispatch | post-v1 | 08, 18 | done — stale label, corrected 2026-09-21: `examples/operator_overloading.em` is a real, working demonstration |
| 41 | `interfaces-and-generics` | Nominal `interface`/`implements` + one bounded generic function, compiled via whole-program monomorphization — no vtables, no runtime type tags | post-v1 | 08, 09 | done — stale label, corrected 2026-09-21: `InterfaceDef`/`implements` shipped and later substantially extended by this session's own plans 88/89 |
| 42 | `enumerable-stdlib` | `Iterable[T]` (`Array[T]`, `Hash[K,V]` — `Range` excluded, see correction note above) plus `map`/`select`/`reduce`/`each_with_index`/`count`/`sum`/`sort`, monomorphized, no chaining | post-v1 | 41, 37, 09 | in-progress (see 2026-09-19 correction below) |
| 43 | `nullable-types-and-safe-navigation` | Finishes `T?`/`&.` (`TYPE_SYSTEM.md` §4); a bare `.method` on `T?` is a compile error; unblocks `\|\|=`/`&&=` | post-v1 | 25, 31 | **superseded by plan 73** — corrected 2026-09-21: `spec/GRAMMAR.md`'s own superseded banner confirms `Option[T]` replaced `T?`/`nil`/`&.` outright rather than this row's own narrower "finish T?" scope ever shipping. Kept as a row rather than deleted, matching this project's own convention for superseded plans |
| 44 | `symbols` | `:identifier` literals, compile-time-interned (dense integer IDs, zero runtime interning table); `Hash[Symbol, T]` keys | post-v1 | 19, 25 | done — stale label, corrected 2026-09-21: `SymbolLit` shipped and used (e.g. `Hash[Symbol,T]` keys) |
| 45 | `stdlib-strings-and-io` | Curated `String` intrinsics (`.length`/`.upcase`/`.split`/`.to_i`/…), `File.read`/`File.write`, `ARGV`/`gets` — `Regexp` explicitly deferred | post-v1 | 19, 36, 44, 12 | done — stale label, corrected 2026-09-21: `File`, `ARGV`/`ARGC`, `gets`, and curated `String` intrinsics all real |
| 46 | `package-manager-and-build` | `emerald.toml` manifest, exact-pin path/git deps, `emerald new/build/run`, `emerald.lock` — no registry, no version-range solving | post-v1 | 23, 27 | done — stale label, corrected 2026-09-21: `emerald.toml`, `emerald new/build/run`, `emerald.lock` all real and exercised (`examples/packages/`, `module_visibility/`) |
| 47 | `repl-and-test-framework` | Incrementally-compiled `emerald repl`; `test`/`assert`/`assert_eq` intrinsics; `emerald test` pass/fail runner — no mocking | post-v1 | 17, 06, 34, 38 | done — stale label, corrected 2026-09-21: `emerald repl` and `emerald test` both real (`repl.rs`/`test_runner.rs`), later extended by plan 80's `property`/`benchmark` syntax |

**Post-shipping correction, found 2026-09-19, not during authoring.** Row
42's commit (`49f9da8`, "feat(stdlib): enumerable Array/Hash methods, array
length header") shipped the array-length-header change (`leaf-array-
length-header` — reused later by plan 65's `ARGV` fix, see `runtime/
emerald_runtime.c`'s own comment at line 616) and real `Array`/`Hash`
indexed get/set, but never delivered the row's own headline. `examples/
README.md`'s current, CI-checked audit — re-verified directly against
source this session, not trusted from the prior note — confirms no
`Iterable[T]` interface exists anywhere in `emerald-sema`, and a block
literal attached to a method call (`arr.select { |x| ... }`) does not
parse at all; block-attachment syntax only works on a user `def` that
declares `&blk` (plan 34). Status corrected above from `planned` to
`in-progress` to reflect what's actually shipped vs. what the commit
message claimed. Plan 70 (below, in the new correctness-before-extension
batch) is this row's real completion, not a duplicate of it.

### Batch: implementing "Beyond the Ceiling" in full (plans 48-57)

After the 36-47 batch was planned, a follow-up analysis ("Beyond the
Ceiling") proposed two further, independent tracks: pure compiler-
implementation improvements that touch no language semantics, and an
actor-model concurrency pillar (BEAM/Pony-inspired) plus algebraic data
types and a `Result[T,E]` error channel. A candidate third idea —
an Effect.ts/id_effect-style `Effect<A, E, R>` type with a built-in
dependency-injection channel — was debated and explicitly rejected: it
would need generic *data types* (plan 41 gives only generic functions),
composition sugar (do-notation/generators, a permanent non-goal here),
a fiber-based concurrency runtime competing with the actor pillar
below, and structural/intersection or variance typing this project has
none of. The user committed to building everything in "Beyond the
Ceiling," and nothing beyond it. These ten plans are that build,
authored together in one pass, same posture as the 28-35 and 36-47
batches. All ten are `planned`.

Sequencing note: 48-51 (implementation) are independent of each other
and of 52-57. 52 (ADTs) and 53 (Result) are a pair — 53 deliberately
does NOT use 52's user-facing `enum` mechanism (which explicitly
declines generic types), instead giving `Result[T,E]` its own hardcoded
compiler-native representation, mirroring `Array[T]`/`Hash[K,V]`/
`Pair[K,V]`. 54→55→56→57 is a strict, deliberately staged concurrency
chain (isolated heaps → real async dispatch → compile-time message
safety → supervision) — each stage is independently provable before
the next adds complexity on top; do not treat 54-57 as parallel-
executable the way most other batches' rows are.

One cross-plan inconsistency surfaced during authoring and was
corrected in place: plan 54 was drafted against a guessed placeholder
runtime API for plan 51's arena mechanism (a single `emerald_arena_new
(capacity)` call) before plan 51's real file landed. Plan 51's actual,
shipped API is three functions (`emerald_region_create`, `emerald_
region_alloc`, `emerald_region_destroy`), with the region handle
retained separately from the allocated pointer. Plan 54 now carries an
explicit "Post-authoring correction" note giving the real call
sequence; its own acceptance criteria are unchanged in substance.

| # | Plan | Scope | Inception §§ | Depends on | Status |
|---|---|---|---|---|---|
| 48 | `incremental-query-compilation` | Wrap parse/check/codegen behind memoized, content-hash-keyed queries (Salsa/rustc-style), whole-file granularity, zero change to observable behavior | post-v1 | 06, 17, 46, 47 | done — stale label, corrected 2026-09-21: a real memoized, content-hash-keyed query layer exists (`emerald-driver/src/cache.rs`), explicitly citing Salsa/`rustc_query_system` as its own design precedent |
| 49 | `parallel-require-compilation` | Level-order parallel compilation of plan 23's require-DAG; restructures codegen to one LLVM `Context`/module per file (inkwell's `Context` isn't `Send`/`Sync`) | post-v1 | 23, 48 | done — stale label, corrected 2026-09-21: real `--jobs` level-order parallel compilation with its own dedicated test suite, leaned on directly by this session's own work (e.g. `jobs_flag_genuinely_controls_concurrency_timed_proof`) |
| 50 | `escape-analysis-stack-allocation` | Conservative intraprocedural escape analysis; a provably non-escaping `.new` instance is stack- (`alloca`) not heap-allocated | post-v1 | 08, 32 | done — stale label, corrected 2026-09-21: a real `EscapeStats`/`stack_allocated` counter with a passing test proving a non-escaping instance is genuinely stack- not heap-allocated |
| 51 | `scope-based-arena-allocation` | A bump-allocated, bulk-freed-on-exit region (`emerald_region_create/alloc/destroy`) for non-escaping allocations (plan 50's verdict) — Zig-style, trigger-agnostic so plan 54 can reuse it for actor heaps | post-v1 | 06/08, 50 | done — stale label, corrected 2026-09-21: `emerald_region_create`/`_alloc`/`_destroy` are real in the runtime; this session's own plans 83/84 build directly on this exact mechanism for borrow-validity |
| 52 | `algebraic-data-types-and-pattern-matching` | Closed, non-generic `enum` sum types (tagged union, `Array`/`Hash`/`Pair`-style hardcoded layout); exhaustiveness-checked `case` extension naming missing variants | post-v1 | 20, 08, 32 | done — stale label, corrected 2026-09-21: real enum sum types with a passing exhaustiveness-check test rejecting a non-exhaustive match |
| 53 | `result-type-and-error-propagation` | `Result[T,E]` as a hardcoded native compound type (not a user `enum` — sidesteps plan 41's generic-types gap); `?` propagation requiring exact `E` match; coexists with plan 38's exceptions as a deliberate two-channel error model (recoverable vs. programmer error) | post-v1 | 38, 42 | done — stale label, corrected 2026-09-21: `Type::Result` is real and used throughout, alongside `?` propagation |
| 54 | `actor-declarations-and-isolated-heaps` | `actor`/`.spawn()` — per-instance arena-backed heap (plan 51); method calls still synchronous in this plan, real dispatch is plan 55's job | post-v1 | 08, 51, 32 (declined) | done — stale label, corrected 2026-09-21: `actor`/`.spawn()` and per-instance arena-backed heaps are real and pervasively used this session (`is_actor`) |
| 55 | `scheduler-and-message-passing` | Honestly scoped down from "M:N" to "N actors over a fixed OS-thread pool" (no coroutine/stack-switching primitive exists); per-actor mailbox, at-most-one-in-flight-message invariant, thread-local fix to plan 38's handler stack | post-v1 | 54 | done — stale label, corrected 2026-09-21: a real mutex/condvar-guarded per-actor mailbox exists in the runtime |
| 56 | `compile-time-message-safety` | "Pony-lite": one linear-use check — a sent reference is legal only if never used again by the sender afterward; not full `iso`/`val`/`ref`/`box`/`tag` capabilities | post-v1 | 55, 51 | done — stale label, corrected 2026-09-21: `check_message_safety` is real, a single canonical mechanism, directly extended and tested extensively by this session's own plans 83/85 |
| — | `top-level-message-and-borrow-safety` (found via this session's "find all bugs" sweep, 2026-09-21) | Real gap in plan 56/83/85: `check_message_safety` and `check_borrow_scopes` were only ever invoked from `check_function_body`/`check_method_body` — a bare top-level `Item::Stmt` (code outside any `fn`/method, i.e. a whole Emerald script's top level) was never checked at all, so an `own`-consumed binding could be silently reused, or two live `borrow var`s of the same binding could silently coexist, at the top level even though the identical pattern is correctly rejected inside a function body. Fixed by adding a `check_program`-level pass over `program.items`' `Item::Stmt`s, calling both checks with fresh top-level `moved`/borrow-state maps. | post-v1 | 56, 83, 85 | done — fix required threading a new `check_wire: bool` parameter through `check_message_safety`/`check_loop_body`/`check_message_safety_stmt`/`check_message_safety_expr_stmt` (`true` for function/method bodies, `false` at the top level), disclosed scope narrowing: plan 60's wire-safety check (no actor reference may cross a message send) is skipped specifically at the top level, because `crates/emerald-cli/tests/actor_concurrency.rs`'s pingpong worked example relies on a mutual-actor-reference idiom (`a.set_peer(b)` / `b.set_peer(a)` at top level) that a real fix would otherwise reject; a full fix would need a real TCP-backed `.register`/`.remote()` rewrite of that test, which the user explicitly deferred in favor of this narrower fix (2026-09-21 AskUserQuestion: "Narrow the fix: skip wire-safety at top level"). Verified: wire-safety is still fully enforced inside function/method bodies (new test `wire_safety_is_skipped_at_the_top_level_but_still_enforced_inside_a_function` asserts both halves). This same fix also exposed that `examples/ownership.em`'s own demonstration code violated plan 83's already-accepted lexical-scope borrow rule (reusing one `Counter` instance across `report`/`increment`/`report` in one flat top-level scope) — a pre-existing issue proven via direct reproduction, not a new regression; fixed by restructuring the example to use separate `Counter` instances per demonstration (output changed from `10\n11\n11\n21\n` to `10\n11\n10\n21\n`, `examples/README.md` and `examples.rs` updated to match). 3 new `emerald-sema` regression tests added. Full workspace re-verified after the fix: 911/911 tests passing (2 skipped), clippy clean, treefmt clean. |
| — | `nested-own-consumption-detection` (found via this session's "find all bugs" sweep, 2026-09-21) | Real, previously-disclosed gap in plan 83's own-consumption check (`check_own_consuming_value`): only a call appearing DIRECTLY as one of `check_message_safety_stmt`'s value-bearing statement positions (a `Let`/`SetField`/`Assign`/... value, or a bare `Stmt::Expr`) was recognized — an own-consuming call nested one level deeper (an arithmetic/comparison operand, a non-`own` argument to an outer call, an array/hash/tuple literal element, a string-interpolation part, ...) fell to a plain `expr_moved_read`, a read rather than a consumption, so the consumed binding was never marked `moved` and a later reuse was wrongly accepted. Concretely: `z: Int64 = identity(consume(w))` (where `consume`'s parameter is `own Widget`) followed by `puts w.value` compiled and ran without complaint before this fix. | post-v1 | 56, 83, 85 | done — `check_own_consuming_value` rewritten as a full recursive walk mirroring `expr_moved_read`'s own exhaustive `Expr` match (a missing arm is now a compile error, not a silent gap), extracting a shared `check_own_consuming_call_args` helper reused by both the `Expr::Call` and `Expr::MethodCall` arms; every non-`own` argument/operand position now recurses back into the same checker instead of degrading to a plain read, so an own-consuming call is caught at any nesting depth. `SafeCall`'s own receiver still isn't given `own`-argument treatment itself (unchanged, undocumented target resolution), but its receiver/args still recurse through the same walk. Verified via a new `emerald-sema` regression test (`rejects_an_own_consuming_call_nested_inside_a_non_own_argument`) AND independently via the real CLI on a standalone `.em` reproduction of the exact `identity(consume(w))` example above, which now correctly fails to compile with a `message-safety` diagnostic naming `w`. Full workspace re-verified after the fix: 912/912 tests passing (2 skipped), clippy clean (one incidental `doc_lazy_continuation` warning from the new doc comment's own wording was hit and fixed in the same change), treefmt clean. |
| 57 | `supervision-trees` | `supervise` blocks, `one_for_one` restart only; an actor's uncaught exception (plan 38, via plan 55's thread-local handler stack) terminates and bulk-frees its arena, supervisor restarts with original spawn args | post-v1 | 55, 38 | done — stale label, corrected 2026-09-21: `supervise`/`one_for_one` are real in both grammar and codegen, with a passing `supervisor_worked_example` test |
| — | `generic-top-level-function-ownership-checking` (found via this session's "find all bugs" sweep, 2026-09-21) | Real, previously-disclosed gap in plan 83's own row: "generic top-level functions don't get `own`/`borrow` param checking yet." Investigation found this was actually THREE compounding bugs, all in `check_generic_function_body`/`infer_expr_type`'s generic-function-call arm, each stemming from the same root pattern — code written against a generic function's parameter types used a bare `resolve_type`/`raw.as_named()` comparison instead of `strip_param_ownership`, which a legal `own`/`borrow`/`borrow var` wrapper never satisfies: (1) `check_generic_function_body`'s own parameter-binding loop hard-rejected ANY ownership annotation on ANY generic function parameter (the type parameter itself, or an ordinary concrete-type parameter alongside it) with "`own T` is only valid as a function or method parameter's own declared type"; (2) even a bare, unwrapped generic type parameter's call-site consistency check (`infer_expr_type`'s `Expr::Call` arm for `gctx.generic_sigs`) used the same bare comparison instead of the fuller `infer_type_param_binding` the generic-METHOD path next to it already correctly used, so an ownership-wrapped argument was invisible to it, always failing with "never uses its own type parameter"; (3) the same call site's `effective_params` computation (used to type-check the call's own arguments) had the identical bug, rejecting the call a second, independent way. All three reproduced directly via the real CLI before any fix (stashing `emerald-sema` and confirming each exact failure), then fixed and re-verified after restoring. | post-v1 | 56, 83, 85 | done — (1) fixed by mirroring `check_function_body`: `check_generic_function_body` now calls `strip_param_ownership` before either the type-parameter-name comparison or the `resolve_type` fallback, and appends `check_message_safety`/`check_borrow_scopes` calls after the body's own ordinary type-check succeeds (previously never called at all, so even a LEGALLY-annotated generic function parameter's `own`-consumption/borrow liveness was completely unenforced). (2)/(3) fixed by switching both call sites to `infer_type_param_binding` (recursing through `TypeExpr::Own`/`Borrow` transparently), matching the generic-method path exactly instead of diverging from it. Disclosed, deliberately NOT extended: a `borrow var`-primitive-parameter's actual MUTATION (the `check_function_body` special case from the earlier `borrow-var-primitive-parameter-mutation` fix) is not opened for a generic function's own type-parameter-typed parameter, since its concrete binding isn't known at this single, pre-monomorphization check — mutating it might or might not propagate depending on what the type parameter eventually binds to, the same "don't promise what codegen can't back up" reasoning already applied to methods. A SEPARATE, newly-found, still-open gap surfaced along the way and left undone here (out of this fix's own scope, to keep it landable as one coherent unit): `check_own_consuming_value`'s own `Expr::Call` arm only ever consults `sigs` (ordinary functions) to detect an `own`-marked call argument, never `generic_sigs` — so calling a GENERIC function with an `own`-typed argument at a call site does not mark that argument consumed at all (verified directly: `consume(w); consume(w);` at the top level, both calls to the same `own T`-parameter generic function, compiles with zero complaint). Two new `emerald-sema` regression tests (`accepts_own_on_a_generic_functions_own_type_parameter`, `rejects_reusing_an_own_consumed_binding_inside_a_generic_functions_own_body` — the latter isolates the now-fixed BODY-level enforcement from the still-open CALL-SITE gap by using a concrete, non-generic `own Widget` parameter alongside the generic one). Full workspace re-verified: 919/919 tests passing (2 skipped), clippy clean, treefmt clean; independently re-verified via the real CLI end-to-end (a `consume[T: Comparable](x: own T)` example compiles, links, and runs correctly, printing the expected output — proving the interaction with `emerald-codegen`'s own pre-existing ownership-stripping-then-generic-monomorphization pipeline composes correctly, not just that sema accepts the annotation).

**Update (2026-09-21, same session, immediately after): the CALL-SITE gap above is now closed too.** `check_own_consuming_value`'s `Expr::Call` arm only ever consulted `sigs` to detect an `own`-marked call argument; fixed by falling back to `generic_sigs.get(name)` when `sigs.get(name)` misses, deriving the same `Vec<Option<Ownership>>` shape via `strip_param_ownership` on each raw parameter. Required threading a new `generic_sigs: &HashMap<String, GenericFunctionSig>` parameter through the entire message-safety call chain (`check_message_safety`/`check_loop_body`/`check_message_safety_stmt`/`check_message_safety_expr_stmt`/`check_own_consuming_value`/`check_own_consuming_call_args`) and all 4 of its external call sites (`check_function_body`/`check_method_body`/`check_generic_function_body`, each already holding `gctx.generic_sigs`; `check_program`'s own top-level pass, which already builds a local `generic_sigs` map) — mechanically identical in shape to this session's earlier `check_wire: bool` threading for the top-level-message-safety fix. Confirmed as a real, pre-existing bug (not assumed) via the real CLI: `consume(w); consume(w);` (two calls to the same `own T`-parameter generic function) compiled with zero complaint before the fix, and is now correctly rejected — the single-call case still compiles and runs (`"consumed"`), confirming no false positive. Two new `emerald-sema` regression tests. Full workspace re-verified: 922/922 tests passing (2 skipped), clippy clean, treefmt clean. |

### Batch: theoretical-ceiling exploration (plans 58-64)

Backfilled into this index 2026-09-19. These seven plans were authored and
shipped as their own batch after 48-57 — plan 65's own file describes them
as "authored together as that batch's theoretical-ceiling exploration" —
but whoever authored plan 65 never added them as rows here, leaving this
document claiming "no further batch is planned beyond [57]" while seven
more plans had already shipped. Rows below are reconstructed from `git log
--oneline --all` (each plan's own `feat(...): ... (plan NN)` shipping
commit) and cross-checked against plan 65's own file, which independently
verified 58-60 against current source before building on them. Status
`done` here means "a real shipping commit for this plan number exists and
plan 65 (for 58-60) or this backfill (for 61-64) found no evidence it was
reverted" — the same, weaker-than-ideal standard named in the disclosure
below, not a fresh acceptance-criteria re-check of every one.

| # | Plan | Scope | Inception §§ | Depends on | Status |
|---|---|---|---|---|---|
| 58 | `generics` | User-declared generic classes (`class Stack[T]`), monomorphized per instantiation — extends plan 41's generic-*function*-only scope to generic *types* | post-v1 | 41 | done |
| 59 | `ffi` | C foreign function interface via `unsafe extern "C" { ... }`; `CString`/`String.from_cstring` | post-v1 | 06 | done |
| 60 | `distributed-actors` | Location-transparent actors over real TCP (`ActorName.remote(addr, name)`); length-prefixed binary wire protocol, per-class wire codecs | post-v1 | 54, 55, 56, 57 | done |
| 61 | `comptime` | Compile-time execution via a tree-walking interpreter | post-v1 | 06, 08 | done |
| 62 | `design-by-contract` | `requires`/`ensures` clauses on method declarations | post-v1 | 38, 08 | done |
| 63 | `purity-annotations` | Checked pure-function annotations | post-v1 | 06 | done |
| 64 | `wasm-codegen-target` | `wasm32-wasi` as a second selectable codegen target alongside native | post-v1 | 06 | done |

### Follow-up: automatic actor placement (plan 65)

Not part of the 58-64 batch's original authoring pass — a separately
authored, direct follow-up refinement to plan 60, debated and written on
its own after 58-64 shipped. Backfilled into this index for the same
reason as the batch above; plan 65's own file is unusually well-verified
(it re-checked plans 52, 53, 55, 57, and 60 against real, currently-
committed source before building on them, citing exact file/line
locations throughout) rather than relying on git log alone.

| # | Plan | Scope | Inception §§ | Depends on | Status |
|---|---|---|---|---|---|
| 65 | `automatic-actor-placement` | `EMERALD_PEERS`/`EMERALD_SELF` env-var discovery; Orleans-style on-demand virtual actor placement via consistent hashing (`ClassName.locate(key, args...)`); every cross-actor send (local and remote) changed to return `Result[Void, SendError]` instead of a `Void`-typed call that threw on remote failure | post-v1 | 51, 52, 53, 55, 57, 60 | done |

**A broader finding, disclosed but deliberately not acted on in this
pass.** While backfilling 58-65, cross-referencing `git log --oneline
--all | grep -oE '\(plan [0-9]+'` against this table found a `(plan N)`
shipping commit for essentially every plan number from 17 through 57 —
meaning most of that range's `planned`/`in-progress` statuses are very
likely stale, the same way row 42's turned out to be. This was **not**
further investigated or corrected here: a commit message naming a plan
is weaker evidence than what row 42's correction and plan 18's own
existing nuanced status ("AST + grammar landed, uncommitted; sema/codegen
not yet touched") required — confirming a row properly means checking
that plan's actual acceptance criteria against current source and current
tests, the way `examples/README.md`'s own audit and plan 65's own
dependency-verification did, not rubber-stamping from `git log` alone.
Scoped out of this pass because 58-65 and row 42 already had that level
of real verification behind them (plan 65's own file; `examples/
README.md`'s audit, respectively) and the other ~35 rows do not yet.
Recommended as a dedicated future audit rather than opened as a numbered
plan here, since its own deliverable would be corrections to this file,
not to the compiler.

### Batch: correctness before extension (plans 66-70)

Authored 2026-09-19, after a session that read `examples/README.md`'s
"Real bugs found while writing these examples" section against current
source rather than trusting either it or any plan's original worked
example. That section discloses several *already-shipped, already-
claimed-done* features that silently fail — not missing features, not
diagnosed-and-erroring features, but ones that compile, run, exit 0, and
produce wrong or missing output with no signal anything went wrong. A
short debate (recorded here rather than left implicit, same posture as
the Effect.ts rejection above) considered three options: bundle these
fixes into whatever the next feature batch turned out to be; open the
scripting-capability question first (subprocess/exec, `ENV`, minimal
JSON/regex — the fork that decides whether Emerald ever aims at being a
viable option for something like a dotfiles-fleet tool); or fix every
disclosed silent-failure bug and finish row 42's real scope first, gating
all new-scope work on it. The user chose the third, explicitly on the
reasoning that a compiler which silently drops output cannot be trusted
enough to be worth extending — matching this project's own "no rounding
up" standard applied to itself. The scripting-vs-narrow identity question
and the no-GC/no-ownership revisit question (`spec/RUNTIME.md` §1) were
both explicitly deferred, not decided, pending a clean compiler to
evaluate either against.

Every bug below is cited against `examples/README.md` and, where checked
further this session, against exact current `crates/emerald-codegen/src/
lib.rs` line numbers — not against a plan record's original claim.

| # | Plan | Scope | Inception §§ | Depends on | Status |
|---|---|---|---|---|---|
| 66 | `puts-and-string-value-flow-codegen` | Root-cause and fix the family of "silently prints nothing, exits 0" bugs: `puts` unusable inside a `def` body, a stored `String` local, a multi-arg `String` intrinsic result, an `Array[String]` index read, and a safe-navigated `String?` after `\|\|=` | post-v1 | 45, 43 | done — **no codegen bug found**; all 5 shapes reproduced correctly, 15-20x each, on current `HEAD`. Root-caused: the original symptom was a nondeterministic runtime race (every compiled program unconditionally spawned/joined `nproc` worker-pool threads at startup, racing glibc's block-buffered stdout), independently fixed by same-session commit `b0d8bc1` before this plan was even authored. Added 7 regression tests (each running the compiled binary 20x, asserting byte-identical correct output) as insurance against the race class, not the AST shapes; corrected `examples/README.md`'s stale bug list accordingly |
| 67 | `string-equality-codegen` | Implement `String == String`/`!=` in codegen — sema already accepts it (plan 45), codegen's comparison lowering has no `(Str, Str)` arm and falls through to its Int64/Float64-only catch-all error | post-v1 | 45 | done — **already implemented**, undocumented: the `(ValKind::Str, ValKind::Str)` arm and a real `emerald_string_eq` (`strcmp`-based, content not pointer equality) runtime primitive both already existed in current source (plan 19-era), just never had a codegen-level regression test. Added the missing test (including the concatenation case, proving content equality) |
| 68 | `generic-method-repeated-print-bug` | Root-cause (currently unknown — disclosed as an open finding in `examples/README.md`, not guessed at) and fix: a second consecutive `puts` of a monomorphized generic method's return value can silently drop its output | post-v1 | 58 | done — **no compiler bug found**. Extensive triangulation (disassembly, 250+ runs, 75 fresh compiles, an in-process Rust test harness) never reproduced it except once, unreproducibly. Root cause identified as a false lead: an artifact of this session's own `ctx_shell` MCP tool silently truncating trailing lines of looped, non-`raw` subprocess output — confirmed via direct `raw:true`-vs-compressed A/B testing across 3 independently-built binaries, 100% correlated with the tool flag, 0% with the compiled program. A `SendFeedback` report was filed on the tool bug itself. Added a 50x-repeat regression test (passes 50/50); corrected `examples/README.md` |
| 69 | `single-file-actor-require-without-jobs` | `require`-splicing a file that declares an `actor` class typechecks and links correctly under `--jobs N` (fixed by plan 65's WeakODR linkage) but still fails typecheck (`unknown type`, `undefined variable`) without `--jobs` — close that remaining half of the gap plan 65 left explicitly open | post-v1 | 23, 65 | done — **real bug, broader than scoped**: this plan's own inherited assumption ("ordinary requires already splice correctly without `--jobs`") was itself wrong — no `require` of any kind (plain function, ordinary class, or actor) was ever spliced by the bare `emerald <file>.em -o out` path; only `emerald build` (via `emerald.toml`) ever called the plan-23 splicer. Fixed in `crates/emerald-cli/src/main.rs`'s `run_legacy`: detect any `Item::Require` up front and route through the existing splicer instead of raw-source compilation, with byte-identical behavior preserved for the (unchanged, far more common) no-`require` case. 3 new regression tests |
| 70 | `enumerable-stdlib-completion` | Row 42's real scope, finished for real this time: `Iterable[T]` interface, block-literal-attached method-call parsing (`arr.select { \|x\| ... }`), monomorphized `map`/`select`/`reduce`/`each_with_index`/`count`/`sum`/`sort` on `Array[T]`/`Hash[K,V]` | post-v1 | 41, 37, 09, 34 | done — **plan 42 had shipped more than documented**: `map`/`select`/`reduce`/`each_with_index`/`count`/`sum`/`sort` on `Array[T]` already worked via a pre-bound named `Proc` argument; the one real, blocking gap was inline block-literal attachment (`arr.select { \|x\| ... }`), which this plan closed via a grammar production plus a post-parse pass that hoists the block into a synthetic named `Proc` and reuses the existing call path unchanged. Real, disclosed narrowings made while implementing, not assumed up front: no `Iterable[T]` compiler-recognized interface was built (deferred — needs `Proc[T,U]` bracketed-type parsing this project doesn't have); `Hash[K,V]`'s `.select`/`.filter` were attempted then removed — their only sensible result type, `Array[Pair[K,V]]`, cannot be written in the current grammar's `TypeName` rule; only an `Ident` receiver gets block-attachment, not an arbitrary expression. `Iterable[T]` remains plan 74's dependency to resolve, not something 70 can hand it |

### Batch: Sable alignment (plans 71-85)

Authored 2026-09-19, alongside
[`2026-09-19T101000Z-inception-2-sable-alignment.md`](./2026-09-19T101000Z-inception-2-sable-alignment.md)
— read that document for the full rationale; this table is the index
entry, same relationship inception.md has to plans 01-15. A second
design brief ("Sable") was brought to the project as Emerald's new
north star, keeping the Emerald name. Four session decisions shape this
batch: a full Rust-style borrow checker (not a lighter escape-analysis
extension), one atomic grammar cutover rather than staged migration,
full replacement of `nil`/`T?` with a real `Option[T]` ADT, and
reopening plan 70's no-chaining decision. Plans 71-74 and 82 are
authored in full; 75-81 and 83-85 are rows only, scoped further when
authored — see the inception-2 document's closing section for why.

| # | Plan | Scope | Inception §§ | Depends on | Status |
|---|---|---|---|---|---|
| 71 | `grammar-unification-fn-and-do-end` | Atomic cutover: `def`→`fn`, `->`→trailing-colon return types, `fn(Args): Ret` function-type syntax, mandatory `do` on `if`/`while`/`for`, `case`/`when`→`match`/`do` pattern-match arms as blocks, the arrow-lambda literal (plan 10) deleted in favor of a `do \|params\| ... end` value assigned to a typed binding, mandatory explicit type annotation on every local binding (no bare `x = expr`) | inception-2 | 06, 10, 20, 29, 52 | done — the named LALR risk resolved cleanly, zero conflicts on two independent clean-rebuild verifications (the bare-`do` lambda is reachable only via `Expr`, never `StmtPrimaryExpr`, mirroring the existing array/hash-literal precedent). The multiple-assignment question resolved itself: `emerald-sema` already required pre-declared, typed targets for both `Assign` and `MultiAssign`, so no carve-out was needed. **Two real gaps found only by actually running the full suite, not by the migration agents' own self-reports**: (1) the migration was scoped to `examples/`/`benchmarks/`/`spec/*.md` and missed ~85+28 embedded `.em` fixtures baked into `emerald-codegen`, `emerald-driver`, `emerald-cli`, and `emerald-mcp`'s own test modules — a class of gap `emerald-sema`'s own migration (done inline, unscoped, by the grammar agent) had already independently hit and fixed; (2) a genuine codegen regression — a lambda's return type is now always the parser's `Void` placeholder (the new syntax carries no return-type annotation), and `emerald-codegen`'s `declare_lambda_functions`/`define_lambda` still trusted that field literally, breaking every non-Void `Proc` used with `.select`/`.map`/etc. Fixed with a narrow, disclosed-scope `ValKind`-level type inference mirroring sema's own `infer_lambda_type`. A separate, unrelated infrastructure issue (a stray 1.6GB build-target directory filling a 2GB `/tmp` tmpfs) produced a misleading, non-reproducible "172 failures" report from the workflow's own verify agent before the real, narrower bugs were isolated — disclosed here so a future session doesn't mistake disk pressure for a code regression again. 784/784 tests passing (2 skipped), clippy and treefmt clean, confirmed independently, twice |
| 72 | `immutable-by-default-bindings` | `var` required for mutation; reassigning a non-`var` binding is a compile error — supersedes plan 31's shipped bare-local reassignment | inception-2 | 31, 71 | done — real decisions recorded, not left implicit: class fields (`@x`) stay freely mutable, unmarked (no `var` slot on `ClassField`, uniform rule, no special-case exemption); function/method parameters and `for`-loop induction variables are unconditionally immutable (no `var` slot exists for either); `\|\|=`/`&&=` (plan 43) brought under the same rule as `=`/compound-assignment, not left as a silent hole. `spec/GRAMMAR.md` §2/§3 and `spec/SEMANTICS.md` §1 corrected — the latter previously claimed "local variables are mutable by default" as current, now marked superseded rather than silently rewritten. 797/797 tests passing (2 skipped), clippy/treefmt clean; independently re-verified beyond the agent's own report by compiling and running both a rejected (non-`var`) and accepted (`var`) reassignment through the real CLI |
| 73 | `option-type-and-nullability-replacement` | `Option[T]`/`Some`/`None` as a real ADT (plan 52's mechanism); `?.`/`??` operators; removes the `nil` `i64`-zero sentinel (plan 25) and supersedes `T?`/`&.`/`\|\|=` (plan 43) outright, not alongside | inception-2 | 25, 43, 52 | done — the project's first generic enum, real new ADT-monomorphization machinery (plan 52's mechanism extended with a `type_params` slot, mirroring `ClassDef`, monomorphized per concrete `T` the way plans 41/58 already do for functions/classes). **Interrupted mid-execution by an API session limit**; resumed cleanly — only one thing was actually broken (`EnumDef`'s new `type_params` field missing from one compiler-synthesized struct literal, `SendError`'s enum item), fixed directly, workspace built clean immediately after. A second agent finished the remaining migration: 10 `emerald-sema` tests deleted (no `Option[T]` analogue — the old `Nil`/`&&=` mechanisms this project declined to carry forward) and 10 rewritten (real invariants preserved under `Option[T]`, e.g. safe-nav/coalesce-on-non-option rejection); 5 `emerald-codegen` fixtures likewise. **A real bug found and fixed, not just untested code**: `String.from_cstring`'s codegen still relied on "a C `NULL` and Emerald's nil sentinel are bit-identical," which was never true for `Option[T]`'s tag+payload representation — silently produced a wrong `Option[String]` tag on a real `strstr` hit; fixed via a proper LLVM `phi` mirroring `Expr::Coalesce`'s own codegen. `examples/c_ffi.em` found stale via a broad sweep the automated tests didn't cover. `spec/GRAMMAR.md`, `TYPE_SYSTEM.md` §4, `SEMANTICS.md` §2 corrected (superseded banners, not full rewrites — flagged by the implementing agent as a real, disclosed scope boundary, then closed). 788/788 tests passing (2 skipped), clippy/treefmt clean; independently re-verified beyond both agents' reports — all three migrated examples (`nullable_safe_nav.em`, `collections.em`, `c_ffi.em`) compiled and run via the real CLI, output confirmed byte-for-byte |
| 74 | `enumerable-chaining` | Amends plan 70 in place: `arr.filter { }.map { }.sort()` in one expression — each `Iterable[T]` method's return type itself implements `Iterable`, superseding plan 70's explicit no-chaining decision | inception-2 | 70, 88, 89 | done — real finding, checked rather than assumed: plan 70's enumerable methods are still hard-coded per-type dispatch, never wired onto plan 89's real `Iterable[T]` interface mechanism (two separate pictures the plan itself asked to be checked against source; confirmed which was true, not guessed). Plan 87's `ChainCallExpr` grammar and plan 70's own sema type-checking already fully supported chaining with zero changes needed — the real gaps were narrower: the parser's block-hoisting pass only ever inspected a chain's outermost call, leaving inner links' blocks un-hoisted; codegen's enumerable-call dispatch required a literal `Ident` receiver with no path for an intermediate chain link's un-named result. Both fixed by extending existing mechanisms (recursive hoisting; synthesizing a fresh named local per chain link, reusing the existing safe-navigation technique), not new ones. A real, disclosed, unrelated pre-existing bug was found and left alone, correctly out of scope at the time: `Hash[K,V].map` returning a `Pair`'s `.value` field failed codegen with no chaining involved at all, confirmed via `git stash` against a clean pre-plan-74 checkout. 809/809 tests passing (2 skipped), clippy/treefmt clean; independently re-verified beyond the agent's own report — a real three-link `.select.map.sort()` chain compiled and run via the CLI, output confirmed correct. **Update (2026-09-21): that bug is gone.** A dispatched follow-up agent, asked to find and fix it, could not reproduce it at all — `examples/enumerable.em` itself already exercises the exact shape (`h.map do |p: Pair[Int64, Int64]| p.value end`) and passes, and two fresh isolated repros plus the existing `plan_70_enumerable_worked_example_linked_and_run_prints_deterministically`/`plan_74_chain_starting_from_a_hash_map_result_compiles_and_runs_correctly` regression tests all pass. Independently re-confirmed directly (not just accepted from the report): compiled and ran `examples/enumerable.em` itself, output `10`/`60`/`2`/`3` for the Hash-related lines matching exactly. Most likely fixed incidentally by later, unrelated codegen work (plans 81/83/84/85 all touched shared codegen paths) rather than by a targeted fix — no code change was needed or made |
| 75 | `multi-trait-bound-generics` | `[T: Ord + Clone]`-style multiple constraint conjunction, extending plan 41's single-bound-only generics | inception-2 | 41 | **superseded by plan 88** — the same `TypeParam.bound` widening plan 88 makes for its own reasons; kept as a row rather than deleted, matching this project's own convention for superseded plans |
| 76 | `import-export-module-visibility` | Explicit `import`/`export` symbol-level visibility, alongside (not necessarily replacing) plan 23's file-based `require` | inception-2 | 23 | done — `export` as a leading keyword on a top-level declaration; `import <path> { Name, Name2 }` as an explicit-import-list form alongside bare `require`. New `Item::Export(Box<Item>)` (wraps rather than adding an `exported` field to six different structs — one Rust exhaustiveness check then found every consumer needing an update) and `Item::Import { path, names }`. Backward compatibility, precisely stated: a file with zero `export` declarations still exports everything (`export_names()` returns `None`, "no restriction," reproducing today's fully-global namespace exactly on a plain `require`); it switches to explicit-export-only *for itself* only once it writes its own first `export` — an `import`/`require` of such a file is a real firewall, not transitively forwarded. Enforcement implemented once as a pure AST walk (`emerald_parser::visibility`, no sema/type info needed) and wired identically into both plan 23's and plan 49's `--jobs` require-graph resolvers; an `import` edge counts as a real leveling dependency the same as `require`. Plan 46's package manager needed no special-casing — a path/git dependency is just another file in the same graph. **Disclosed gap, since closed (2026-09-21)**: `emerald-cli/src/main.rs`'s `run_legacy` originally gated multi-file resolution on `Item::Require` alone, so a file using only `import` with zero bare `require` got no multi-file resolution via the plain `emerald <file>.em` form — its `requires_present` check now also matches `Item::Import { .. }`, routing through the exact same `require::resolve_program_with_hashes` path (which already handled `Item::Import` correctly since plan 76 itself, this was purely a one-line detection gap). New `examples/module_visibility/import_only.em` exercises this exact shape (an import-only file, zero bare `require`s) — the original example always paired `import` with a `require`, so it never actually tested this path. Independently re-verified beyond that fix's own report: compiled and ran directly via the plain (no `--jobs`) CLI form, output `7`/`12` confirmed. **Remaining disclosed gap, not yet closed**: the visibility walk still covers call/construct expressions but not `TypeExpr` positions (a signature merely naming an unexported class, never constructed/called, isn't caught). 907/907 tests passing (2 skipped), clippy/treefmt clean; independently re-verified beyond the agent's own report — `examples/module_visibility/` compiled and run via the real CLI (`Hello, Emerald!`/`7`), and a hand-written import of a genuinely non-exported symbol (routed through the real require-graph path, not the disclosed gap's un-resolved path) produced the real, specific, non-panicking rejection message directly, not just trusted from the report. Built concurrently with plan 78 (still in flight); the new `Item` variants required plan 78's `emerald-fmt` crate to add its own `Export`/`Import` handling, coordinated via a direct message mid-flight rather than left to collide silently

**Update (2026-09-21, this session's "find all bugs" sweep): the `TypeExpr`-position gap above is now closed.** `crates/emerald-parser/src/visibility.rs`'s `Checker` gained `check_type_name`/`walk_type` — a second, narrow walk (no `locals` set, since a type name is never a local-variable binding) recursing through every `TypeExpr` wrapper (`Generic`/`Tuple`/`Func`/`Own`/`Borrow`) to find every `Named`/`Generic`-base type name referenced, wired into every real position: a function/method's own parameters, return type, and splat-parameter element type; a class's superclass name, `implements` interface name and its own generic arguments, and field types; an actor's field types; an enum variant's field types; a newtype's underlying type; an interface's own required-method signatures. `TypeExpr` carries no span of its own, so a violation found this way reports `(0, 0)` — the same "no better span available" fallback `emerald-sema`'s own diagnostics already use pervasively — a real, disclosed narrowing of its own (a precise expression-level span isn't available here the way it is for the pre-existing `Stmt`/`Expr` walk). Confirmed as a real, pre-existing bug (not assumed) by reverting the fix on a stashed copy of `emerald-parser` and confirming a three-file reproduction (a function whose own parameter type names an unexported class from a required file, with a body that never constructs or calls anything cross-file) compiled AND ran without complaint before restoring the fix. Since both of this workspace's require-resolution call sites (`emerald-cli::require`, `emerald-driver::require_graph`) already share this one `visibility::find_violations` implementation (this plan's own original design choice), the fix applies to both — including the `--jobs`-parallel driver path — with no changes needed at either call site. Two new regression tests in `emerald-cli::require`'s own test module (`a_function_signature_naming_an_unexported_cross_file_type_is_rejected`, and its accept-side sibling proving the fix doesn't false-positive on a legitimately exported cross-file type). Full workspace re-verified: 921/921 tests passing (2 skipped), clippy clean, treefmt clean. |
| 77 | `doc-comments-and-emerald-doc` | `##` doc comments, reserved distinct from plan 20's `#`; an `emerald doc` extractor/renderer | inception-2 | 20, 46 | done — `##`-comment capture is a dedicated pre-parse line-scan (`collect_doc_comments`, mirroring the existing `Contract.text`/`assert`-rewrite idiom) rather than a lexer-token, since the real, live lexer is LALRPOP's own `match {}` block (`emerald-lexer` turned out to be dead, unreferenced plan-02 prototype code, confirmed before touching anything, left alone), which already discards all comments as insignificant whitespace before any production runs. A byte-offset map is threaded into the grammar as a second parameter; every declaration production (`fn`/`class`/`interface`/`module`/`enum`/`actor`) looks up its own leading position, correctly yielding `None` across a blank line or an intervening `#` comment. New `pub doc: Option<String>` field on `Function`/`ClassDef`/`InterfaceDef`/`ModuleDef`/`EnumDef`/`ActorDef`, propagated through monomorphization (generic class/enum/method instantiation, actor→`ClassDef` normalization) so a doc comment survives a generic copy. New `emerald doc <file.em|dir> [-o out.md]` (`doc_runner.rs`, mirrors `test_runner`/`bench_runner`'s structure) renders one combined Markdown doc with each declaration's real Emerald signature, not a paraphrase. Disclosed scope cuts: interface *methods* don't get their own per-method doc field (only the interface itself); `unsafe extern "C" { }` blocks aren't documentable. 848/848 tests passing (2 skipped), clippy/treefmt clean; independently re-verified beyond the agent's own report — `emerald doc examples/doc_comments.em` run through the real compiled CLI, output read directly and confirmed correct (blank-line-orphaned and plain-`#`-commented declarations correctly absent), and the example itself compiled and run, output byte-for-byte matching. Built concurrently in the same working tree as plans 78/79 (three-agent parallel dispatch, first of this session); `crates/emerald-cli/src/main.rs`'s subcommand dispatch and the workspace `Cargo.toml` carry all three plans' wiring. **Correction**: a later plan-78 edit to the shared `main.rs` did in fact clobber this plan's and plan 79's `mod`/match-arm lines (only plan 78's own survived) — caught by the repo's pre-push gate failing 4 `doc_subcommand` tests, not by either agent's own report; fixed directly in a small follow-up commit (`6e18380`) restoring the missing lines, independently re-verified again afterward |
| 78 | `canonical-formatter` | `emerald format` — a real pretty-printer for Emerald *source*, not the compiler's own Rust (treefmt/rustfmt are unrelated to this) | inception-2 | 46 | done — a pure AST pretty-printer (`crates/emerald-fmt`): parses via `emerald_parser::parse_named`, re-emits canonical text purely from the `Program` AST, no whitespace-preserving pass. Comments are a real, disclosed limitation: ordinary `#` comments have no AST representation and are lost; `##` doc-comment blocks (plan 77's `doc: Option<String>` field) round-trip faithfully since they're real AST data. Three non-obvious correctness traps found and fixed by the implementing agent before this was safe to ship: (1) `unless`/`until` desugar into `Expr::Not` wrapping an unrestricted condition, which this grammar's total lack of parenthesized-expression syntax means must always be reprinted via `unless`/`until` itself, never `if !cond`, or reparsing silently changes the tree; (2) compound assignment (`+=` etc.) desugars into a right-nested `BinOp` unlike every ordinarily-parsed (left-associated) binary tree, so it must always be reprinted via the compound operator; (3) the parser's own enumerable-block-hoisting pass (plan 74) synthesizes a `__enum_blk_N: Proc` statement with a real inferred return type with no surface syntax — printing the hoisted form separately would silently lose that type, so an `unhoist_enumerable_blocks` pass reconstructs the original inline-block call first. **A fourth bug found only by this session's own independent verification, not the implementing agent's**: `write_method_call` unconditionally omitted `()` on a zero-arg method call, but this grammar accepts a parenless zero-arg call only for a user class's own methods — a builtin-type intrinsic like `Int64.abs()` is rejected without the parens ("method call on non-class type"), so a formatted file using `.abs()` failed to recompile. Fixed directly (not by the implementing agent): always emit explicit parens on a call, the one spelling valid in both cases, since the formatter has no type information at this stage to know which case applies. 863/863 tests passing (2 skipped), clippy/treefmt clean; independently re-verified beyond the agent's own report, twice — once via the agent's own worked example, and again after the zero-arg-parens fix via a fresh hand-written messy file exercising a builtin-intrinsic call, confirmed idempotent and confirmed the reformatted output still compiles and runs correctly through the real CLI. Built concurrently with plans 76/77/79 (three-then-four-way parallel dispatch across this session); its own edit to the shared `crates/emerald-cli/src/main.rs` briefly clobbered plans 77/79's subcommand wiring, caught by the repo's pre-push gate and fixed directly — see plan 77's row for the correction |
| 79 | `canonical-linter` | `emerald lint` — a first-party static-analysis/style checker for Emerald source | inception-2 | 46, 22 | done — `emerald lint <file.em|dir>`, built directly on `emerald-parser`'s AST alone, no `emerald-sema` dependency needed. Four rules, each grounded in something real about this language rather than generic filler: `unused-local-variable` and `needless-var` (a `var` never reassigned — plan 72's own immutable-by-default philosophy), `empty-rescue` (Ruby's swallowed-exception anti-pattern), `empty-class` (no fields/methods/superclass — the superclass guard deliberately spares the common empty-exception-subclass idiom). Deliberately conservative flat-scope evaluation ("used/reassigned anywhere in scope," not "textually after") chosen to produce false negatives rather than false positives, verified by hand-reading all 27 real `.em` files under `examples/` (including `packages/`, `parallel/`) before writing any rule. Output format mirrors the compiler's own miette diagnostics (rule name, severity, file:line:col, source snippet with caret); exit code non-zero on any finding. No new `pub` export needed from `emerald-parser`/`emerald-sema`. 848/848 tests passing (2 skipped), clippy/treefmt clean; independently re-verified beyond the agent's own report — `emerald lint examples/` run through the real compiled CLI, confirmed "31 files checked, 0 findings" directly, not just trusted from the report. Built concurrently alongside plans 77/78 (three-agent parallel dispatch); see plan 77's row for the shared-file (`main.rs`/`Cargo.toml`) coordination note |
| 80 | `property-and-benchmark-test-syntax` | `property "..." do ... end` and `benchmark "..." do ... end` as first-class syntax alongside plan 47's `test` | inception-2 | 47 | done — both mirror plan 47's existing `test` mechanism end to end rather than inventing new ones. `benchmark` gets a real new `emerald benchmark <file>.em` subcommand measuring CPU time (`clock()`, matching `benchmarks/REPORT.md`'s own stated methodology, portable to wasm32-wasi unlike `CLOCK_PROCESS_CPUTIME_ID`) around each block, via a synthetic `extern "C"` FFI primitive (plan 59's mechanism, not a new codegen builtin). **`property` is explicitly, disclosedly NOT real property-based testing** — no input generator/shrinker exists; it runs its body exactly once, syntactic sugar over `test`, named plainly in its own doc comments and this record rather than left to look more capable than it is. `emerald property <file>.em` is a literal alias for `emerald test`, documented as identical. A real, disclosed finding while writing the benchmark example: one of its two blocks (array traversal over compile-time-known data) reports near-zero time because LLVM's `default<O3>` folds it to a closed form — the same constant-folding pitfall `benchmarks/REPORT.md`'s own Headline §3 already documents for the `sum` benchmark, confirmed to recur here and disclosed in the example itself rather than silently kept. 815/815 tests passing (2 skipped), clippy/treefmt clean; independently re-verified beyond the agent's own report — both new subcommands run directly against the real CLI, correct pass/fail and timing output confirmed, and the ordinary compile path's rejection of `property`/`benchmark`/`test` blocks confirmed directly |
| 81 | `domain-types-and-units` | Lightweight newtype-style domain types (`Meters`, `MetersPerSecond`); lowest-confidence item in the Sable brief itself (\"illustrative, not finalized\") | inception-2 | 41, 58 | done — the brief itself leaves this open ("how to make domain types easy without boilerplate"); the concrete design decision made and implemented: `newtype Meters: Float64` (one line, no `end`, mirrors `enum`'s shape) declares a real, nominally distinct wrapper around exactly one primitive (`Int64`/`Float64`/`String`/`Boolean`/`Symbol`), deliberately its own mechanism, not `class` reuse — `class` instances carry a `self` pointer no zero-cost value can have. `Name.new(value)` reuses the existing `.new` grammar production verbatim (already general over any identifier, no grammar change needed there); `.value` unwraps via an ordinary zero-arg method call special-cased in sema/codegen. Two different newtypes over the *same* underlying primitive (`Meters`/`Seconds`, both `Float64`) are mutually non-interchangeable, and neither interchanges with the bare primitive — enforced via a new `Type::Newtype(String, Box<Type>)`. **Zero-cost proven, not asserted**: `value_kind_for_type` resolves a newtype name to its underlying primitive's own LLVM representation before any type/signature decision is made, so `.new`/`.value` compile to the bare argument/receiver with no extra instruction; `examples/newtype_zero_cost_benchmark.em` (plan 80's real `emerald benchmark` mechanism) sums 2,000,000 real, non-folded elements over `Array[Float64]` vs. `Array[Meters]` — independently re-run twice by this session, both times statistically indistinguishable (`Meters` occasionally faster, pure noise), not just accepted from the report. **A real, pre-existing-pattern bug found and fixed along the way**: `Expr::Compare`'s class-`==` guard shared `local_classes` with newtypes and wrongly matched newtype-typed operands too. **Operator overloading explicitly declined and disclosed** (`spec/GRAMMAR.md` §15, `TYPE_SYSTEM.md` §11) — the existing mechanism compiles every method with a leading `self` pointer a zero-cost newtype value never has; extending it would be new machinery, not a small extension. Disclosed gap: a `class`/`actor` *field* typed as a newtype has no `.value` dispatch wired (only `Let` locals and function/method params do) — not exercised by the example. 876/876 tests passing (2 skipped), clippy/treefmt clean; independently re-verified beyond the agent's own report — `examples/domain_types.em` run via the real CLI (`10.4384`/`1`/`100`, byte-for-byte matching), a hand-written cross-newtype call (`Seconds` where `Meters` expected) and a hand-written raw-`Float64`-where-`Meters`-expected call both produced real, specific, non-panicking sema rejections naming the exact types involved, and the zero-cost benchmark re-run twice directly. **Two small conventions the implementing agent missed, filled in directly rather than dispatching a follow-up agent for something this small**: `examples/README.md`'s coverage table had no row for either new example; `crates/emerald-cli/tests/examples.rs` had no durable regression test for `domain_types.em` (the benchmark file correctly has none, matching plan 80's own `benchmark_example.em` precedent) |

**Update (2026-09-21, this session's "find all bugs" sweep): the disclosed "a class/actor field typed as a newtype has no `.value` dispatch wired" gap above is now closed.** `build_method_call` (`emerald-codegen`) only ever recognized `.value` on a newtype via `local_classes` (keyed by a named local variable) — an `@field.value` receiver (`Expr::InstanceVar`) fell straight through to the ordinary `Expr::Ident`-only guard and failed with "method calls are only supported on a plain local-variable receiver," even though sema fully accepted the program (confirmed as a real, pre-existing bug, not assumed, by reverting the fix on a stashed copy and reproducing the exact error via the real CLI before restoring it). Fixed by mirroring this session's own earlier Proc-typed-field-`.call` fix: a new `Expr::InstanceVar` case in `build_method_call` resolves the field's newtype-ness from `field_classes` (`build_class_layout`'s pre-existing `f.ty.to_string()` fallback already stores a newtype field's bare name there — no change needed on that side) and returns the same zero-cost identity `build_expr(recv)` the local-variable arm already uses. New `emerald-codegen` regression test (`newtype_typed_class_field_value_unwrap_from_inside_the_declaring_class_works`). Full workspace re-verified: 916/916 tests passing (2 skipped), clippy clean, treefmt clean. Disclosed, narrower scope matching the Proc-field precedent: only `@field.value` from INSIDE the declaring class's own method body is covered — `instance.field.value` from outside the class is a separate, unrelated limitation (this grammar has no public field read-access expression at all outside a `read`-sugared accessor method).
| 82 | `ownership-model-design` | Design-only: decide `own`/`borrow` syntax, interaction with plans 50/51's regions and the actor-per-instance-region model, interaction with generics (75) and plan 72's mutability, interaction with FFI's region-bound-return convention (discussed, not yet planned, in this session's earlier FFI conversation). Output is a spec document, not code | inception-2 | 50, 51, 54, 72 | done — `spec/OWNERSHIP.md`. Four parallel research passes (Rust, Austral, Vale, Pony) independently converged on a smaller hybrid than "full Rust checker"; that finding was surfaced and the explicit decision, made anyway, was to proceed with the full model. Design response to the evidence: `own`/`borrow`/`borrow var` keywords (no sigils), no named lifetime parameters — borrow validity is expressed relative to plans 50/51's existing regions instead — and v1 deliberately targets Rust's pre-2018 lexical-scope checker, not NLL, citing Polonius's own 8-years-and-counting timeline as evidence flow-sensitive checking is too large a first bite |
| 83 | `borrow-checker-sema-enforcement` | Lexical-scope liveness checking in `emerald-sema` for `own`/`borrow`/`borrow var`: reject multiple live `borrow var` or a `borrow`+`borrow var` conflict on one binding within its lexical scope, reject use of an `own`-consumed binding, reject a `borrow` outliving its named region — full scope in `spec/OWNERSHIP.md` §10 | inception-2 | 82 | done — `TypeExpr` gained `Own(Box<TypeExpr>)`/`Borrow(Box<TypeExpr>, is_mut)`, composing with the existing recursive `TypeExpr` shape (plan 88) the same way `Tuple`/`Func` already do; legal only as a function/method parameter's own declared type — `resolve_type` hard-rejects it everywhere else (fields, `Let`, return types, nested inside another type) with a real diagnostic. All four §10 rules implemented: (1)/(2) borrow/borrow-var liveness via a new `check_borrow_scopes` pass, deliberately flatter than per-block — one whole function body is treated as a single lexical scope, a real, disclosed *further* conservative narrowing on top of §8's already-conservative model (never under-rejects, occasionally over-rejects independent borrows in mutually-exclusive `if`/`else` arms); (3) own-consumption generalizes plan 56's existing `is_cross_actor_send`/moved-map mechanism directly, from "only checked at a cross-actor send" to "checked at every `own`-typed call argument," per §2's own instruction — not a second, parallel mechanism; (4) a `borrow` return type is rejected outright, citing the real mechanism (plan 51's per-call-frame region is destroyed at the returning function's own exit, strictly before a returned borrow's scope extending into the caller could still be live). **Disclosed codegen passthrough, precisely located**: `crates/emerald-codegen/src/lib.rs`'s `strip_ownership_annotations_in_items`, called once at the top of `compile_to_object_impl` before any other codegen logic sees `items` — rewrites every function/method's params and return type, stripping `own`/`borrow`/`borrow var` down to the plain type, so downstream codegen never sees the wrapper at all; `own T`/`borrow T`/`borrow var T` all compile exactly like plain `T` today. Plan 84 replaces this with real raw-pointer borrow codegen and `own`-invalidation codegen — independently confirmed by reading the actual disclosed comment at the source, not just trusting the report's description of it, and it matches exactly. Disclosed gaps: `check_borrow_scopes`/`check_own_consuming_value` only run inside function/method bodies (pre-existing plan-56 architecture) — top-level `Item::Stmt` sequences get zero rule-1/2/3 checking; own-consumption is only recognized directly in a statement's value position, not nested deeper (`foo(consume(d))`); generic top-level functions (not methods) don't get `own`/`borrow` param checking yet. 888/888 tests passing (2 skipped), clippy/treefmt clean; independently re-verified beyond the agent's own report — `examples/ownership.em` run via the real CLI (`10`/`11`/`11`, byte-for-byte matching), and all four rejection rules hand-verified with fresh, independently-written invalid files (not the agent's own test fixtures) producing real, specific, non-panicking diagnostics naming the exact binding and rule violated — one authoring mistake in this session's own rule-2 test (initially aliased two distinct parameter bindings at a call site rather than testing the same binding twice, which this lexical/nominal checker correctly doesn't catch, matching its disclosed non-flow-sensitive scope) was caught and corrected before concluding the rule itself was sound |
| 84 | `deterministic-destruction-codegen` | Narrower than originally named, per `spec/OWNERSHIP.md` §9's explicit decline of a Rust-style `Drop` mechanism: codegen for `own`-parameter passing (reusing plan 56's existing invalidation codegen) and zero-cost `borrow`/`borrow var`-parameter passing (a raw pointer, no wrapper) — no new destructor mechanism is built | inception-2 | 83 | done — `own` needed no new codegen at all (confirmed, not assumed: it's sema-only, mirroring plan 56's `is_cross_actor_send`, which also turned out sema-only). `borrow`/`borrow var` of an already-pointer type (a class instance, `String`) fully erases to the plain type — genuinely zero-cost, since a class instance was already passed by pointer before this plan touched anything. `borrow`/`borrow var` of a BY-VALUE primitive (`Int64`/`Float64`/`Boolean`/`Symbol`) is the real new mechanism this plan builds: `strip_ownership_in_type_expr` wraps the stripped type in a synthetic marker for top-level `fn`s only (methods are a disclosed, out-of-scope gap — `build_method_call`'s separate arg-building path was never touched); `bind_params` loads the real scalar through the incoming pointer into an ordinary local, and for `borrow var` registers a writeback that fires at all 5 real function-exit points, reusing the exact `alloca` of a plain local argument (zero extra allocation) or spilling to one new `alloca` for a non-local argument (the one real, disclosed cost, proven at the IR level by a dedicated test, not asserted). **A real, disclosed limitation found, not introduced**: a `borrow var` PRIMITIVE has no legal syntax to actually be mutated in valid Emerald source today — plan 72's own "every parameter is immutable by construction, no `var` slot exists regardless of which annotation binds it" rule makes `x = ...` a compile error for every parameter, `borrow var`-typed or not (a mutating CLASS method like `Counter.bump` sidesteps this by writing a *field*, never reassigning the parameter binding itself — no equivalent exists for a primitive with no fields/methods). The real codegen is proven correct and end-to-end sound via `emerald-codegen`'s own test suite (which parses source directly and bypasses sema, the same pre-existing idiom every test in that file already uses), but genuinely cannot be exercised through a real, sema-checked `.em` program — closing this in `emerald-sema` is real, disclosed future work, not attempted here (deliberately out of this plan's own crate scope). **Zero-cost claim, re-examined rather than accepted at face value**: this session's own independent re-runs of `examples/ownership_zero_cost_benchmark.em` (4 separate runs, `borrow Int64` vs. plain `Int64`, 20,000×1,000 calls) showed a modest, inconsistent gap — three runs +5% to +9% slower for `borrow Int64`, one run within noise — not the clean parity the implementing agent's own report claimed. At these microsecond-scale absolute durations this could be measurement noise or a small, real, undisambiguated per-call pointer-indirection cost; recorded here precisely rather than repeating "statistically indistinguishable" uncritically. Correctness (not performance) is what's fully proven: 896/896 tests passing (2 skipped) including 8 new codegen-level regression tests (mutation visibility for `Int64`/`Float64`/`Boolean`, IR-level proof of the `ptr` signature and of zero extra allocas for a local argument, `own` no-copy IR proof, a class-typed `borrow var` regression guard), clippy/treefmt clean; independently re-verified beyond the agent's own report — `examples/ownership.em` (extended with a `describe(x: borrow Int64)` case) run via the real CLI, output `10`/`11`/`11`/`21` confirmed byte-for-byte |

**Update (2026-09-21, this session's "find all bugs" sweep): the disclosed "no legal mutation syntax" gap above is now closed**, for the top-level-function case this plan's own codegen already supported. `check_function_body` (`emerald-sema`) now special-cases a `borrow var`-annotated by-value parameter (`Int64`/`Float64`/`Boolean`/`Symbol`) as mutable — via `declare_local` instead of a bare `env.insert`, reusing the exact machinery an ordinary `var`-declared local already gets — leaving every other parameter exactly as immutable as before. `check_method_body`'s identical parameter loop deliberately does NOT mirror this: a method's by-value `borrow var` parameter still keeps the original full-erasure passthrough (no real pointer, no writeback), so opening the same syntax there would silently accept code whose mutation never reaches the caller — confirmed still rejected by a new test. `examples/ownership.em` extended with a real `increment_primitive(n: borrow var Int64)` demonstration; verified via the real CLI (`10`/`11`/`10`/`21`/`42`, the new `42` proving the caller's own `counter_value` local was mutated through the pointer, not just a local rebind) and 3 new `emerald-sema` regression tests (accept at top level; still reject a plain `borrow`; still reject a method parameter). Full workspace re-verified: 915/915 tests passing (2 skipped), clippy clean, treefmt clean. **Still genuinely open**: extending the real pointer/writeback treatment to class/actor/module methods themselves (`build_method_call`'s separate, multi-arm argument-building code was not touched — same scope boundary this plan originally disclosed).
| 85 | `ownership-actor-ffi-integration` | Implements `spec/OWNERSHIP.md` §5 (borrow checker and plan 56's linear-use check coexist as separate layers, mirroring Rust's own borrow-checker/`Send`-`Sync` split — neither subsumes the other) and §7 (FFI `own`/`borrow` typing at `extern` boundaries with region-transfer semantics) | inception-2 | 84, 59, 60 | done — **process note**: the first two dispatched agents for this plan independently refused to proceed, flagging this repo's lean-ctx tool-routing mandate as a possible prompt-injection/tool-hijacking attempt. Investigated directly rather than dismissed: confirmed via this session's own consistent, cross-validated multi-hour track record (real commits pushed to the real GitHub remote, real passing test counts at every step) and independently-corroborating structural evidence (this session's own available-skills list includes files like `hermes-tool-routing-hooks`/`activate-tooling`/`mcp-tool-selection` describing the identical "block native tools, force MCP routing" practice as a broad, pre-existing convention of this user's other real projects, not something novel to this session) that this was a false positive, not a live attack — confirmed with the user directly before proceeding. Implemented directly by the orchestrating session itself rather than a third dispatched agent, since the refusal was a structural fresh-agent-context problem, not something re-explaining could fix without looking more suspicious each time. **Part A (§5)**: verified, not just asserted, that plan 56's actor-send check and plan 83's own-consumption check compose correctly — and *why*, by construction rather than luck: an actor method must be `Void`-returning (cross-actor calls are asynchronous), so an actor send can only ever appear as a bare `Stmt::Expr`, never nested inside a `Let`/`Assign`/`Return` value position, the only shape `own`-consumption's own annotation-driven check ever reaches — the two mechanisms structurally never compete for the same call. Also verified the borrow checker's own liveness rules apply correctly inside an actor's method bodies (`check_method_body` runs the identical pass for both). The already-named future-work gap (a `ref`-shaped value escaping via a second alias, e.g. `b: LogMessage = a` then sending only `a`, leaving `b` un-poisoned) was reproduced directly, confirmed real exactly as §5 predicted, and correctly left alone — the full 3-tier `iso`/`ref`/`val` capability system it would take to close is explicitly out of this plan's scope. **Part B (§7)**: `own T`/`borrow T` made legal on an `unsafe extern "C"` fn's own RETURN type only (a real, disclosed scope narrowing — extern PARAMETERS stay exactly as strict/unannotated as before this plan); new `FunctionSig.return_ownership` field, resolved via `resolve_extern_type` reusing `strip_param_ownership` (a second real call site for a function whose own doc comment previously said "legal only for params"). `own` needs no new enforcement (a fresh FFI-returned value is the existing "trivially fresh" bucket). `borrow`/`borrow var` gets one real, new check — the call's result cannot be bound to a `Let`/`SetField`/`Assign` (would let the reference outlive the call itself, exactly what §7's own "bound to a region the caller guarantees outlives the call" text forbids), verified by both an accept (used directly, inline) and two reject cases (bound to a `Let`; reassigned to a `var`). Codegen: `emerald-codegen`'s `strip_ownership_annotations_in_item` gained an `Item::Extern` arm — always plain erasure (`allow_borrow_ptr_marker: false`), never the real pointer+writeback machinery plan 84 built for parameters, since an extern return's annotation is pure documentation-as-code with zero effect on the actual C ABI call. New `examples/ffi_ownership.em`: `strdup` (real glibc, allocates a fresh copy) declared `own CString`; `strstr` (real glibc, points into its `haystack` argument's own storage — already `examples/c_ffi.em`'s own worked example, left untouched rather than risk desyncing its own separately-hardcoded Rust test) declared `borrow CString`, used only inline. 906/906 tests passing (2 skipped), clippy/treefmt clean; independently re-verified at every step while writing this plan myself rather than accepted from a report — both real-CLI compiled-and-run demonstrations, and every accept/reject case, tested directly, including catching and fixing two of my own test-authoring mistakes along the way (an unrelated `puts CString` limitation masking a real check; a top-level-statement test that trivially passed because plan 56/83's own pre-existing "top-level statements aren't checked" gap made it exercise nothing at all). Also found and fixed, in passing: plans 83/84's own `ownership.em`/`ownership_zero_cost_benchmark.em` examples were missing from `examples/README.md`'s coverage table (a repeat of the same small-convention gap already caught and fixed once in plan 81's row) |

**Mid-session addendum, plan 86.** While plan 71 was executing, a real
inconsistency surfaced: plan 70 shipped block-attached calls
(`arr.select { \|x\| ... }`) using brace syntax only, which sits oddly
next to this batch's own do...end-unification direction (plan 71,
citing Sable §44/§50). The explicit decision, when raised: keep both
delimiters, Ruby-style (braces for short blocks, `do...end` for longer
ones), not do...end-exclusivity — but that decision still requires a
real grammar addition, authored below as its own plan rather than
folded silently into 70 or 71.

| # | Plan | Scope | Inception §§ | Depends on | Status |
|---|---|---|---|---|---|
| 88 | `structured-type-expressions` | Replaces `TypeName`'s flat-string convention (`Array[Int64]`, `Hash[K,V]`, etc. as grammar-level string concatenation) with a real recursive type-expression AST; gives `Proc` a real written parameterized form (closing plan 74's actual blocker — `sema::Type::Proc(Vec<Type>, Box<Type>)` already exists, it just can't be written as an annotation, only inferred from a co-located lambda); widens generic-parameter bounds to conjunction, absorbing plan 75; lifts the explicit "generic methods are not supported" sema restriction | inception-2 | 09, 41, 58 | done — parser/sema work fully real and independently verified: a genuine recursive `TypeExpr` AST (`Named`/`Generic`/`Tuple`/`Func`) replaces the flat string, `Array[Array[Int64]]`-style nesting now parses when it structurally couldn't before, `resolve_type` rewritten to consume structure directly (simpler, not more complex), `Proc[Args..., Ret]` chosen as Proc's written form (zero new grammar productions — reuses the existing bracketed-generic shape), multi-bound conjunction (`[T: A + B]`) implemented and tested for the rejection case (the accept case is bottlenecked by a real, pre-existing, out-of-scope limit: a class has only one `implements` clause, so no type can currently satisfy two bounds at once — disclosed, not hidden). **A real regression was found and closed before commit, not shipped**: lifting the sema-level "generic methods not supported" rejection made sema accept generic methods it could type-check but codegen could not compile — a call reaching codegen crashed the LLVM verifier instead of getting the old clean rejection, reproduced directly by this session. Closed with a dedicated codegen-level gate (a small, disclosed scope: sema's real monomorphization/type-checking work stays and is tested; codegen refuses a generic-method call with a clean, honest diagnostic instead of proceeding to broken IR). **Also found, disclosed, and correctly left for follow-up rather than silently worked around**: `InterfaceDef` has no type-parameter clause at all and is capped at one method — the plan's own worked example (`interface Iterable[T]` with a generic `map[U]`) cannot literally parse today, a gap this plan's own todo list never anticipated; and codegen's `.call` dispatch only resolves a receiver that is a top-level `Let`-bound lambda `Ident` — calling a `Proc` through any parameter (function or method) still fails. 796/796 tests passing (2 skipped), clippy/treefmt clean; independently re-verified beyond both agents' reports — the working `Proc[Array[Int64], Int64]` end-to-end case, and the now-clean (not crashing) generic-method diagnostic, both reproduced directly against the real CLI |
| 89 | `generic-method-codegen-and-interface-generics` | Closes what plan 88 left open after its own worked example was actually tried end-to-end: `InterfaceDef` gains a type-parameter clause and more than one method (today capped at zero type params, one method); real per-call-site codegen monomorphization for generic methods, replacing plan 88's stopgap rejection; codegen's `.call` dispatch extended to any `Proc`-typed value, not only a top-level `Let`-bound lambda `Ident` | inception-2 | 88 | done — the actual target, `interface Iterable[T]` with a generic `map[U](f: Proc[T, U])` method implemented by a real class and called with a real lambda, compiles, links, and runs correctly end to end (independently reproduced by this session, printing the exact specified `2`/`6`). Real monomorphization reuses the existing top-level generic-function pipeline's strategy, driven lazily per call site since a method's own type parameter can bind to a primitive, not just a class. Proc values now carry their own function pointer in a heap-block header, enabling genuine indirect calls through any parameter/local/top-level-let — not just the old literal-lambda-only path. Two real, disclosed narrowings, not regressions (nothing coded these before either): a Proc-typed *class field*'s `.call` isn't supported (`.call`'s receiver must still be a plain `Ident`); a class-bound (non-Proc-inferred) generic method like `pick[T](a: T, b: T): T` still doesn't codegen — independently reproduced by this session, confirmed it now fails with a clean, specific diagnostic ("could not resolve generic method's type parameter to a concrete type"), not a crash. 805/805 tests passing (2 skipped), clippy/treefmt clean; independently re-verified beyond the agent's own report — the full worked example and the narrower failure case both reproduced directly against the real CLI. **Update (2026-09-21): the Proc-typed class-field `.call` gap is closed.** Root cause, found by direct investigation, not guessed: `build_class_layout` encoded a field's plain type STRING into `field_classes` (`f.ty.to_string()`), never the `proc_sig_for_type`-encoded signature `bind_params` already produces for a Proc-typed PARAMETER — so `@op.call(x)` (`Expr::InstanceVar`, not `Expr::Ident`) hit `build_method_call`'s generic "only supported on a plain local-variable receiver" guard before ever reaching the `.call`-specific dispatch at all. Fixed in two places: `build_class_layout` now calls `proc_sig_for_type` for fields, matching the parameter convention exactly; `build_method_call` gained a new `Expr::InstanceVar` + `method == "call"` special case (mirroring the pre-existing actor-`InstanceVar` check immediately above it) that resolves the field's signature from `field_classes` and dispatches through a newly-extracted `build_indirect_proc_call` helper — factored out of the original Ident-receiver path so both real call sites (an ordinary Proc-typed local/parameter, and this new class-field case) share one copy of the actual indirect-call codegen rather than two independently-maintained ones. Disclosed, narrower scope, not attempted: only `@field.call(...)` (from inside the declaring class's own method) is covered — `instance.field.call(...)` from OUTSIDE the class hits a separate, unrelated limitation (this grammar has no public field read-access expression at all outside a `read`-sugared accessor method), confirmed directly rather than assumed. New regression test `proc_typed_class_field_call_from_inside_the_declaring_class_works` (`emerald-codegen`); `examples/closures.em` extended with a real `Adder` class demonstrating it. 908/908 tests passing (2 skipped), clippy/treefmt clean; independently re-verified — the exact reproduction case (`@op.call(x)` inside `Adder#apply`) compiled and ran directly via the real CLI both before (confirmed the disclosed failure was real and current) and after the fix (confirmed `42`), and the extended `examples/closures.em` run end to end, output `15`/`42`/`0`/`1`/`2`/`42` matching exactly |

**Update (2026-09-21, this session's "find all bugs" sweep): the "class-bound (non-Proc-inferred) generic method still doesn't codegen" gap above is now closed.** Root cause: `infer_method_type_param_binding` (the codegen-side re-derivation of `emerald-sema`'s own fuller structural unifier) handled only the `Proc[..., U]`-in-return-position shape — a generic method's type parameter bound by an ORDINARY, non-`Proc` argument (`fn identity[U](x: U): U do x end`) had no matching arm at all, so `resolve_generic_method_instance` always failed with "could not resolve generic method's type parameter to a concrete type," even though sema's own `infer_type_param_binding` (`TypeExpr::Named(name) if name == param_name`) already accepted the identical program — confirmed as a real, pre-existing bug, not assumed, by reverting the fix on a stashed copy and reproducing the exact error via the real CLI before restoring it. Fixed by extending `infer_concrete_type_from_arg` (previously lambda-only) to also recognize a fresh `ClassName.new(...)`, a handful of literal shapes (`Int64`/`Float64`/`Boolean`/`Symbol`/`String`), and — the common case — a plain named local, resolved via `local_classes` first (for a class/newtype receiver) then falling back to the local's own `ValKind` (threaded as a new parameter through `infer_method_type_param_binding`/`resolve_generic_method_instance`, both of which needed it only for this fix). New `emerald-codegen` regression test (`a_class_bound_generic_method_with_a_plain_non_proc_type_parameter_works`). Full workspace re-verified: 917/917 tests passing (2 skipped), clippy clean, treefmt clean. Disclosed, still-real narrowing (not attempted here): a type parameter nested inside `Array[U]`/`Hash[K, U]`/... or bound by an argument that is itself a nested call result/field read/binary expression is not covered — this only re-derives sema's leaf-argument cases, not general structural unification.
| 86 | `block-attached-call-do-end-alternative` | `arr.select do \|x\| ... end` as a second, equally valid spelling for plan 70's existing brace-only block-attached-call sites — additive, not a replacement; must execute only after plan 71's `grammar.lalrpop` rewrite is stable (both touch the same file) | inception-2 | 70, 71 | **superseded by plan 87** — the "keep both" decision this plan was authored under was reversed before execution began; file stands unedited as the historical record of that earlier decision |

**Further mid-session correction: plan 86 itself was superseded before
being executed.** The direction changed from "keep both delimiters,
Ruby-style" to "`do...end` is the one and only way to express a block in
Emerald" — surfacing two real ambiguities (a while/if condition whose
own expression carries an attached block; `do...end`'s traditionally
loose binding conflicting with plan 74's chaining ambition) that plan 87
resolves structurally rather than by convention.

| # | Plan | Scope | Inception §§ | Depends on | Status |
|---|---|---|---|---|---|
| 87 | `do-end-exclusive-block-attachment` | Removes plan 34's `{ \|params\| body }` and plan 70's `.method { blk }` entirely, replacing both with `do \|params\| ... end` exclusively; a block-attached call used as a while/if condition requires explicit parens (a real grammar restriction with its own diagnostic, not a convention); a `do...end` immediately followed by `.method` binds tightly to that call (chain-local, not full Ruby-style global looseness) — needed for plan 74 once unblocked. Must execute only after plan 71's `grammar.lalrpop` rewrite is stable | inception-2 | 34, 70, 71 | done — zero LALR conflicts (designed around up front via a parallel `CondExpr`→`CondPrimaryExpr` condition-ladder used only by `if`/`while`/`match`/`requires`/`ensures`, not discovered via a failed build); chain-local tight binding implemented via a self-recursive `ChainCallExpr` nonterminal. Independently re-verified beyond the agent's own report: both migrated examples (`enumerable.em`, `closures.em`) compile and run via the real CLI matching documented output; the disallowed bare condition-position case is a real parse error; the parenthesized escape hatch parses (confirmed only after correcting a test mistake of my own — forgetting plan 70's pre-existing block-param-needs-an-explicit-type restriction). Disclosed gap: the disallowed case's error is LALRPOP's generic "unrecognized token," not a hand-authored diagnostic — a custom error production there would reintroduce the exact conflict this plan avoids. 790/790 tests passing (2 skipped), clippy/treefmt clean |

### Completion (post-v1)

Open-ended — this track has no fixed end state. Revisit this section's
`in-progress`/`planned` rows whenever a new post-v1 plan lands or an
existing one ships. The 36-47 batch's own completion criterion is
narrower and closed: that batch is done when all twelve rows above are
`done` — row 42's real completion is now plan 70, per the correction
above — at which point Emerald's language/stdlib surface should sit at
roughly the ~45% Ruby-parity ceiling described above, a deliberate
plateau, not a step toward 100%. The 48-57 batch's completion criterion
is likewise closed: done when all ten rows above are `done`, at which
point every proposal in the "Beyond the Ceiling" analysis is shipped.
That was **not**, in fact, the end of this project's roadmap, despite
what this document previously claimed here — 58-65 shipped afterward
(backfilled above), and the 66-70 correctness batch opens the next one.
The 66-70 batch is now `done`, all five rows — though not the way any
of them predicted: two of the five (66, 68) turned out not to be real
compiler defects at all (a since-independently-fixed runtime race, and
a tooling artifact in this session's own shell-output-compression MCP
tool, respectively), one (67) found its fix already shipped and only
missing a test, and the other two (69, 70) found real work whose true
scope differed from what was planned (69 broader, 70 narrower on
`Iterable[T]`/`Hash` filtering). `examples/README.md`'s "Real bugs
found" section was corrected in place (struck through, not deleted, per
this project's own precedent) rather than coming back literally empty.
**Real, disclosed consequence for plan 74**: it depends on plan 70
delivering `Iterable[T]`, which 70 explicitly did not build — plan 74
cannot proceed as scoped until that interface exists, a genuine blocker
surfaced by execution, not by planning. What comes after 66-70 —
the scripting-vs-narrow identity question and the memory-model revisit,
both explicitly deferred above — is deliberately undecided as of this
writing, not silently open-ended.
