[workspace]
resolver = "2"
members = ["crates/app"]

# Single version and shared metadata for all workspace crates (inherit with version.workspace = true).
[workspace.package]
version = "0.1.0"
edition = "2021"
license = "CC-BY-SA-4.0"
